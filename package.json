{
  "name": "zeebox",
  "displayName": "Zeebox",
  "description": "Personal version of gruvbox",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Themes"
  ],
  "contributes": {
    "themes": [
      {
        "label": "Zeebox",
        "uiTheme": "vs-dark",
        "path": "./themes/Zeebox-color-theme.json"
      }
    ]
  },
  "scripts": {
    "start": "nodemon --watch src src/index.js",
    "build": "node src/index.js && mkdir -p build",
    "prepublishOnly": "npm run build && vsce publish"
  }
}